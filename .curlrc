# Follow redirects
--location

# Show error on HTTP failures
--fail

# Connection timeout (seconds)
--connect-timeout 10

# Max time for entire operation (seconds)
--max-time 60

# Use a sane user agent
--user-agent "curl"
